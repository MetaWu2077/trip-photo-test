name: trip_photo_test
description: 旅拍照片上传测试
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.4
  tencent_cos: ^1.0.8
  image: ^4.1.3
  path_provider: ^2.1.1
  permission_handler: ^11.1.0
  flutter_dotenv: ^5.1.0
  http: ^1.2.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - cos.local.env
